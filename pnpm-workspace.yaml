allowBuilds:
  sharp: true

# pnpm 11 holds newly published packages for 24 hours by default. This exact
# prerelease was intentionally selected and verified while testing the metadata
# image cache fix, so keep the policy enabled for every other dependency.
minimumReleaseAgeExclude:
  - vinext@1.0.0-beta.6
